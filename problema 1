#GENERADOR DE CONTRASEÑAS
#Usando la librería random y una función que contenga las letras minusculas, mayusculas (sin tilde), números y simbolos 
#Las organice de una manera aleatoria y muestre una contraseña del tamaño asignado 


#Solucion
import random
num=int(input('determine la longitud que desee para su clave'))
def contraseña(num):
    clave = ''
    for i in range(num):
        clave+= random.choice('abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789,;.:-_{[}]+*!"#$%&/()=?')
    return clave
print(f'Su nueva clave es: {contraseña(num)}')
